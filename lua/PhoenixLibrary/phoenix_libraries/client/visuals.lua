--[[	For heatwave trails and stuff like that...stuff that should be managed outside of internal workings, as to be persistant]]module("visuals",package.seeall)local Visual = {	}function Register(strVisual,tblMeta) --Accept custom meta loading from phoenix_objects	endfunction Update(strVisual,strUnique,...)endfunction GetData(strVisual) --For thermalvision to retrieve bullet heat trails	endhook.Add("Think",NAME,function ()	end)hook.Add("RenderScreenspaceEffects",NAME,function ()	for strVisual, tblInstances in pairs(Visual) do			endend)